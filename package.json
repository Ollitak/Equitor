{
  "name": "full-stack-open-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:frontend": "cd client && npm install",
    "dev:frontend": "cd client && npm start",
    "dev": "nodemon index.js",
    "build:frontend": "rm -rf build && cd client && npm run build && mv build ../. ",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^14.1.0",
    "express": "^4.17.2",
    "express-history-api-fallback": "^2.2.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.6",
    "mongoose-unique-validator": "^3.0.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "nodemon": "^2.0.15"
  }
}
